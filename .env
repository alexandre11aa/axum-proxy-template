# URL da API que vamos consumir (fonte dos dados)
# API de teste: Fake Store API (produtos fake, sem auth, sem rate limit)
SOURCE_API_URL=https://fakestoreapi.com/products

# Porta local (Render define isso automaticamente em produção)
PORT=3000
